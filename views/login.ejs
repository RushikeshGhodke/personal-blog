<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/style.css" />
    <title>Login Page</title>
  </head>
  <body>
    <div
      class="noto-sans"
      style="text-align: left; max-width: 400px; margin-left: 20px"
    >
      <h2>Admin Login</h2>

      <% if (error) { %>
      <div class="error"><%= error %></div>
      <% } %>

      <form
        style="
          display: flex;
          flex-direction: column;
          align-items: flex-start;
          gap: 10px;
        "
        action="/login"
        method="POST"
      >
        <input
          style="width: 100%"
          type="text"
          name="username"
          id="username"
          placeholder="Username"
          required
        />
        <input
          style="width: 100%"
          type="password"
          name="password"
          id="password"
          placeholder="Password"
          required
        />
        <button type="submit">Login</button>
      </form>
      <form action="/" method="GET">
        <button type="submit">Back to Blog</button>
      </form>
    </div>
  </body>
</html>
