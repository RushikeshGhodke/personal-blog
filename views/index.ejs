<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
  <link rel="stylesheet" href="/style.css">
</head>
<body class="noto-sans">
  <header>
    <h1>Rushikesh's Personal Blog</h1>
    <p>Sharing thoughts and ideas about technology</p>
  </header>

  <main>
    <% if (blogs && blogs.length > 0) { %>
      <div>
        <% blogs.forEach(blog => { %>
          <article>
            <% if (blog.slug) { %>
              <a href="/blog/<%= blog.slug %>"><%= blog.title %></a>
            <% } %>
          </article>
        <% }) %>
      </div>
    <% } else { %>
      <div>
        <h2>No blog posts yet</h2>
        <p>Check back later for new content!</p>
      </div>
    <% } %>
  </main>
</body>
</html>
