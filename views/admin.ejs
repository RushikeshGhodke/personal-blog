<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title><%= title %></title>
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body>
    <main class="noto-sans">
      <div>
        <h2>Admin Dashboard</h2>
        <p>Welcome, <%= username %>!</p>
        
        <form action="/logout" method="POST" style="display: inline;">
          <button type="submit">Logout</button>
        </form>
        
        <a href="/">← Back to Blog</a>
        
        <h3>Manage Blog Posts</h3>
        
        <% if (blogs && blogs.length > 0) { %>
          <div>
            <% blogs.forEach(blog => { %>
              <div style="border: 1px solid #ccc; margin: 10px 0; padding: 10px;">
                <h4><%= blog.title %></h4>
                <p style="font-size: 12px; color: #666;"><%= blog.date %></p>
                <p><%= blog.content.substring(0, 100) %>...</p>
                
                <div>
                  <a href="/blog/<%= blog.slug %>">View</a>
                  
                  <form action="/admin/delete/<%= blog.slug %>" method="POST" style="display: inline; margin-left: 10px;" onsubmit="return confirm('Are you sure you want to delete this blog?')">
                    <button type="submit" style="background: #e74c3c; color: white; border: none; padding: 5px 10px;">Delete</button>
                  </form>
                </div>
              </div>
            <% }) %>
          </div>
        <% } else { %>
          <p>No blog posts found.</p>
        <% } %>
      </div>
    </main>
  </body>
</html>
